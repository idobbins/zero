version: '3'
name: zero
services:
  db:
    image: postgres:16
    container_name: zero_db
    environment:
      POSTGRES_USER: zero
      POSTGRES_PASSWORD: password
      POSTGRES_DB: zero
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data: